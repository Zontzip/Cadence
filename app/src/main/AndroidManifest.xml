<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zontzor.cadence" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".views.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.UserProfileActivity" >
        </activity>
        <activity android:name=".views.UserRidesActivity" >
        </activity>
        <activity android:name=".views.UserGoalsActivity" >
        </activity>
        <activity android:name=".views.CadenceCalcActivity" >
        </activity>
        <activity android:name=".views.RideAddActivity" >
        </activity>
    </application>

</manifest>
